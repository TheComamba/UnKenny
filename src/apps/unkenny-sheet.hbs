<form class="flexcol" autocomplete="off">
    <section>
        <header class="flex-header">
            <h1>
                Edit UnKennyness Parameters
            </h1>
        </header>
    </section>
    <section>
        <h2>
            Preamble
        </h2>

        <textarea name="preamble" rows="20">{{preamble}}</textarea>

        <div class='form-field'>
            <label>Model</label>
            <select name='model' class='form-control'>
                {{#each models}}
                <option value="{{this.name}}" {{#isSelected}}selected="selected" {{/isSelected}}>{{this.name}}</option>
                {{/each}}
            </select>
        </div>

        <div class='form-field'>
            <label>Prefix response with "/talk" macro from "Talking Actors" module</label>
            <input name="prefixWithTalk" type="checkbox" {{#prefixWithTalk}}checked{{/prefixWithTalk}}>
        </div>

        <button type="submit">
            <i class="fa fa-check"></i>
            Save
        </button>
    </section>
</form>